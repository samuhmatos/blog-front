export interface PostCommentReportParamsApi {
  post_id: number;
  reason: string;
  comment_id: number;
}

export interface PostCommentReportParams {
  postId: number;
  reason: string;
  commentId: number;
}
